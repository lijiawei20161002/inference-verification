# Pure-numpy core (sampling, verifiers, harness, metrics, signal, spec_decode)
# and the dependency-free tests. `python tests/test_smoke.py` needs only this.
numpy>=1.24

# Real-model GPU backend (ivgym/backends/hf_gpu.py) -- required by every
# experiments/exp_*_gpu.py; needs a CUDA host. torch is the only heavy dep --
# reuse a system install via:  python -m venv --system-site-packages .venv
torch>=2.4
transformers>=4.44
accelerate
safetensors

# Figures and the paper build (experiments/plot_*.py, paper/make_figs.py).
matplotlib>=3.7

# The cross-validated proxy panel in experiments/exp_proxy_distance_grid.py.
scikit-learn>=1.3

# The gradient-boosted aggregator probe in experiments/exp_int8_gbt_aggregation.py
# (native Booster API only -- it does not need the sklearn wrapper).
xgboost>=2.0

# Optional: the llm_judge Tier-0 verifier (experiments/exp_io_detector_gpu.py).
# Its key resolves from ANTHROPIC_API_KEY, the repo .claude/, or
# ~/.claude/anthropic_key.sh. Set IVGYM_JUDGE=0 to skip it entirely.
# anthropic

# Optional: the higher-throughput vLLM production path (vllm_adapter.py).
# vllm>=0.10.0

# Optional: pytest, if you want `pytest tests/` instead of running each file.
# pytest
